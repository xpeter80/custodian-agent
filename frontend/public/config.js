// API Configuration for Frontend
// This file is not bundled so it can be modifying after deployment.
window.APP_CONFIG = {
    // Use relative path so reverse proxy maps it to the backend container
    apiUrl: '/api'
};
